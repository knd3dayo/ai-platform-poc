# A-03-03_テスト再現評価ハーネスの検証

## 検証目的

本検証の主目的は、サブ課題 A-03-03「テスト・再現・評価ハーネス」について、PoC 環境で成立性を確認することである。

最終的には、A-03 の完了判定に必要な材料として、自律実行結果を自動テスト、再現実行、成果物レビューで点検できるかを明確にすることを目指す。

## 対応する課題とサブ課題

| 親課題 | サブ課題 | この文書で主に確認すること |
| --- | --- | --- |
| A-03 | A-03-03 | 自律実行結果を自動テスト、再現実行、成果物レビューで点検できるかを確認する。 |

必要に応じて、副次的に A-02-02、A-03-02、A-04-05、A-04-06、O-02-04 の前提整理にも利用する。

## 関連するアーキテクチャ検討文書

- [技術課題と対応方針](../03_検証準備/01_技術課題と対応方針.md)
  - A-03-03 に対応し、自律実行結果を自動テスト、再現実行、成果物レビューで点検できることを確認する。
- [生成AIアプリケーション層の実現方式](../02_アーキテクチャ実現方式/02_生成AIアプリケーション層の実現方式.md)
  - 細粒度 HITL より成果物レビューや PR ベースの承認を優先する整理を参照する。
- [AIエージェントの業務適用を見据えた生成AIアプリケーション層の検討.md](../01_アーキテクチャ検討/02_AIエージェントの業務適用を見据えた生成AIアプリケーション層の検討.md)
  - ハーネスエンジニアリング、自動テスト、再現性確保の理論的根拠を参照する。
- [A-02-02_Resumeプロトコルの検証.md](./A-02-02_Resumeプロトコルの検証.md)
  - trace_id と再現実行の足場として参照する。
- [A-04-05_自律型コーディングエージェント呼び出しの検証.md](./A-04-05_自律型コーディングエージェント呼び出しの検証.md)
  - coding-agent 系の自律実行結果点検との接続先として参照する。
- [A-04-06_自律型DeepAgents実装の検証.md](./A-04-06_自律型DeepAgents実装の検証.md)
  - DeepAgents 系の自律実行結果点検との接続先として参照する。

## 検証で確認したいこと

### 1. 正常系

- 自律実行の結果を自動テストや静的検査で点検できること。
- trace_id、設定、入力条件から再現実行の足掛かりを再構成できること。
- 書き込み系や高自律タスクでは、成果物レビューまたは PR ベースの承認ゲートへ接続できること。

### 2. 異常系

- 自動テストや成果物レビューを経ずに高リスク成果物を昇格しないこと。
- 再現条件が不足したまま品質判定や承認を行わないこと。
- テストが通らない、またはレビュー根拠が不足する成果物を黙って accept しないこと。

### 3. 運用系

- どのテスト、どのレビュー観点、どの承認者が結果判定に関与したかを追跡できること。
- 自動テスト、再現実行、成果物レビューの責務分担を文書化できること。
- A-03-02 の停止条件と矛盾しない形で評価ハーネスを配置できること。

## 対象構成

| 観点 | 主な既存実装 / 入口 | 備考 |
| --- | --- | --- |
| 自動テスト | pytest、回帰 test、smoke test | 実装ごとの最小品質保証 |
| 再現実行 | trace_id、config、audit log、resume 契約 | 実行経路の再現に利用 |
| 成果物レビュー | PR、差分確認、承認記録 | 高リスク成果物の昇格ゲート |
| 対象実装 | coding-agent、DeepAgents、SV/自律型入口 | A-04-05 / A-04-06 と接続 |

## 前提条件

- 自律実行結果に trace_id、設定ファイル、audit log などの再現材料が残ること。
- 自動テストまたは成果物レビューへ接続できる実行形態を利用すること。
- 書き込みや副作用を伴う成果物について、承認主体と承認単位を定義できること。

## 検証手順

### 1. 事前準備

```bash
# 既存の回帰 test、audit log、trace_id 利用箇所を洗い出す
rg -n "trace_id|audit|pytest|approval|review|pull request|PR" \
  ${HOME}/source/repos/ai-chat-util/app/src \
  ${HOME}/source/repos/ai-platform-poc/docs
```

期待結果:

- 再現実行と成果物レビューの接続点を列挙できる。
- 実装ごとのテスト・レビュー責務を整理できる。

### 2. 正常系確認

```bash
# representative scenario の実行結果について、テスト・再現・レビューの各経路を確認する
# 具体コマンドは対象実装ごとのハーネス確定後に記入する
```

期待結果:

- 少なくとも 1 つの自律実行について、実行結果をテストまたはレビューへ接続できる。
- 再現実行に必要な trace_id や設定値を回収できる。

### 3. 異常系確認

```bash
# テスト失敗、レビュー未実施、再現材料不足のケースを比較する
# 具体コマンドは対象実装ごとのハーネス確定後に記入する
```

期待結果:

- レビュー未実施や再現不能な成果物を accept しない運用条件を明文化できる。
- 高リスク成果物の昇格条件が曖昧なままにならない。

## 判定基準

| 観点 | 判定基準 |
| --- | --- |
| 構造成立性 | 自動テスト、再現実行、成果物レビューの接続点が文書化されている。 |
| 制御成立性 | テスト未通過、レビュー未実施、再現材料不足の成果物を accept しない条件を説明できる。 |
| 運用成立性 | trace_id、audit log、設定、差分レビューを用いて結果判定の根拠を再構成できる。 |

## 検証結果記録欄

| 項目 | 結果 | 補足 |
| --- | --- | --- |
| 正常系 | 未記入 |  |
| 異常系 | 未記入 |  |
| 運用系 | 未記入 |  |

## 残課題

- coding-agent 系と DeepAgents 系で共通に使える成果物レビュー基準の標準化が必要である。
- trace_id から設定・実行ログ・成果物差分まで辿る再現手順の標準化が必要である。
- 昇格基準との接続は A-03-04 で別途整理する。