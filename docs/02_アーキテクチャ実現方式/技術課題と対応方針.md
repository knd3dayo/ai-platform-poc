# 生成AIアプリケーション基盤の技術課題と対応方針

## 0. この文書の位置づけ

本書は、`docs/01_アーキテクチャ検討` 配下の一次文書を基準に、生成AIアプリケーション基盤の主要な技術課題と対応方針を整理する文書です。

ここでの課題は、PoC の進捗メモではなく、**アーキテクチャとして先に定義しておくべき論点**を対象とします。そのため、従来の古い課題番号や PoC ベースのステータス分類は引き継がず、一次文書の責務定義に合わせて再編します。

本書では、課題が **どの層の課題か、または全体の課題か** が一目で分かるよう、カテゴリを明示します。

---

## 1. 課題の分類方針

本書のカテゴリは次の 5 区分です。

| カテゴリ | 意味 |
| --- | --- |
| 全体・横断 | 3 層をまたいで成立する共通課題 |
| Application層 | WF型 / SV型 / 自律型、状態管理、ハーネスに関する課題 |
| Tool層 | MCP、実行インターフェース、意味付け、Read / Write 制御に関する課題 |
| AIガバナンス層 | Guardrails、根拠性、評価、停止判断に関する課題 |
| 周辺実行基盤 | BFF、Event Bus、状態管理DB、Observability など補助コンポーネントの課題 |

**補足**

* API Gateway や BFF は 3 層そのものではなく、責務を成立させる周辺実行基盤として扱います。
* マルチエージェントの East-West 統制のように、単一層へ閉じない論点は「全体・横断」または「AIガバナンス層」を主カテゴリに置き、必要に応じて注記でまたがりを明示します。

---

## 2. 技術課題一覧

| # | カテゴリ | 技術課題 | 要点（対応方針） |
| --- | --- | --- | --- |
| 1 | 全体・横断 | North境界と責務分離 | API Gateway、BFF、3 層、周辺基盤の責務を混同せず、決定論的防御、入口オーケストレーション、意味論的統制を分離する。 |
| 2 | 全体・横断 | `trace_id` と Trajectory の一気通貫追跡 | BFF で `trace_id` を確定し、Application層、Tool層、AIガバナンス層、評価基盤、通知経路まで一貫して伝播する。 |
| 3 | 全体・横断 | 認証と権限コンテキスト継承 | 実行ユーザーの認証コンテキストを end-to-end で引き継ぎ、AI が固定権限で越権しない構造にする。 |
| 4 | Application層 | WF型 / SV型 / 自律型の使い分け | 予見性に応じて型を選び、Coordinator で適切な経路へ振り分ける。 |
| 5 | Application層 | 非同期HITLと状態永続化 | 承認待ちや長時間処理を同期応答で抱え込まず、Checkpointer と再開可能な状態設計を標準化する。 |
| 6 | Application層 | ハーネスエンジニアリングと段階的自律化 | 自律性の高さに応じてサンドボックス、停止条件、予算上限、テストを強化し、段階的に自律化する。 |
| 7 | Tool層 | MCP による標準化された実行インターフェース | 個別 API 直結を避け、MCP 等の標準プロトコルで再利用可能な Tool 資産を整備する。 |
| 8 | Tool層 | セマンティックレイヤーによる意味付け | 生データや生 API を直接 AI に見せず、ビジネス用語で意味付けしたビューやメタデータを返す。 |
| 9 | Tool層 | ステートレス実行と非同期ジョブ化 | Tool層では状態を持たず、即時結果または Job ID を返し、待機や状態管理は Application層へ委譲する。 |
| 10 | Tool層 | Read / Write 分離と Dry Run | 更新系ツールを参照系と分離し、Dry Run と承認前提の実行を標準化する。 |
| 11 | AIガバナンス層 | 入出力 Guardrails とモデル利用統制 | 自然言語入出力、モデル利用経路、Tool 利用上限、予算を企業共通ポリシーとして統制する。 |
| 12 | AIガバナンス層 | グラウンディング、サイテーション、共通評価 | 根拠付けとサイテーションを生成プロセスに組み込み、同期検証と事後評価を併用する。 |
| 13 | AIガバナンス層 | Risk-Adaptive 監督と Kill Switch | LLM-as-a-Judge や共通評価を用い、リスクの高いケースだけを HITL や停止へ接続する。 |
| 14 | 全体・横断 | マルチエージェントの East-West 統制 | agent-to-agent handoff、共有 State、予算、再帰的ブロッキングをゼロトラストで扱う。 |
| 15 | 周辺実行基盤 | Event Bus、状態管理DB、Checkpointer、Observability の分離 | 疎結合な事実通知、UI 向け状態、AI 内部状態、Trajectory 監視を別の補助基盤として設計する。 |

---

## 3. 課題別の対応方針

### 3.1 全体・横断の課題

#### 課題1: North境界と責務分離

**背景**

一次文書では、3 層アーキテクチャは責務による分離であり、API Gateway や BFF はその責務を成立させる補助コンポーネントとして扱われています。ここを曖昧にすると、入口防御、業務状態管理、意味論的統制が同じ場所に混ざります。

**対応方針**

* API Gateway は JWT 検証、WAF、レート制限などの決定論的防御に限定します。
* BFF は `trace_id` 確定、Fast / Slow Track 分岐、状態管理DB 書き込み主権、通知、Resume 受付を担います。
* AIガバナンス層は自然言語入出力、モデル利用、根拠性、共通評価、停止判断を担います。

#### 課題2: `trace_id` と Trajectory の一気通貫追跡

**背景**

AI システムの監査対象は個別ログではなく、思考、判断、ツール実行、承認待ち、再開から成る Trajectory です。

**対応方針**

* BFF で `trace_id` を確定します。
* Event Bus、Application層、Tool層、AIガバナンス層、Observability 基盤へ一貫して伝播します。
* `trace_id` を、再開や通知、評価結果検索に使う横断キーとして扱います。

#### 課題3: 認証と権限コンテキスト継承

**背景**

AI が固定の高権限で動く構成は、越権アクセスや監査不能を招きます。

**対応方針**

* 実行ユーザーの認証コンテキストを end-to-end で継承します。
* AIガバナンス層は Tool 利用の上限制御を行い、Tool層は本人権限に基づく最終認可を受けます。
* 共有 API キー中心ではなく、ユーザーコンテキスト中心の設計に寄せます。

#### 課題14: マルチエージェントの East-West 統制

**背景**

単一エージェントの入口検査だけでは、agent-to-agent handoff、共有 State、再帰的合議による事故は防げません。

**対応方針**

* handoff 前に、委譲先権限、操作種別、予算、共有 State の完全性を検査します。
* 共有 State には State Scrubbing を適用し、不要な文脈や機密情報を削減します。
* セッション単位で反復回数と予算を監視し、再帰的ブロッキングを抑止します。

### 3.2 Application層の課題

#### 課題4: WF型 / SV型 / 自律型の使い分け

**背景**

すべてを単一のエージェント構成で処理しようとすると、制御不能と過剰設計の両方を招きます。

**対応方針**

* 予見性の高い工程は WF型で固定します。
* 合議、評価、承認が必要な難所は SV型へ寄せます。
* 探索的工程だけを自律型へ委譲します。
* Coordinator により入口で型を選びます。

#### 課題5: 非同期HITLと状態永続化

**背景**

人間承認はリアルタイムで返るとは限らず、同期 API の中で待機させる設計は破綻します。

**対応方針**

* 承認直前で `interrupt` を発火し、Checkpointer へ状態を保存します。
* Resume は `thread_id` をキーに実行します。
* UI 向け状態は状態管理DBに分離し、AI 内部状態と混同しません。

#### 課題6: ハーネスエンジニアリングと段階的自律化

**背景**

自律型を安全に使うには、プロンプトよりも周辺環境の設計が重要です。

**対応方針**

* WF型には入力検証と再実行性、SV型には状態永続化と非同期HITL、自律型にはサンドボックスと停止条件を必須要件として持たせます。
* 段階的自律化を前提に、Read-Only、限定権限、成果物レビューから始めます。

### 3.3 Tool層の課題

#### 課題7: MCP による標準化された実行インターフェース

**背景**

個別 API の直結は、アプリごとのラッパー乱立と再利用性低下を招きます。

**対応方針**

* Tool 公開は MCP 等の標準プロトコルを基本とします。
* Application層はツール利用判断に集中し、実行仕様の詳細は Tool層へ閉じ込めます。

#### 課題8: セマンティックレイヤーによる意味付け

**背景**

生データや生 API を直接 AI に見せると、解釈ミスやハルシネーションを引き起こします。

**対応方針**

* データ基盤側で意味付けしたビューや API を利用します。
* Tool 定義の schema や description に業務用語、制約、出典メタデータを含めます。
* 物理スキーマではなく、ビジネス概念で接続します。

#### 課題9: ステートレス実行と非同期ジョブ化

**背景**

Tool層で状態や承認待ちを持つと、Application層との責務分界が崩れます。

**対応方針**

* Tool層は即時結果または Job ID を返します。
* 待機、再試行、承認待ち、通知は Application層で扱います。
* Tool層内部での HITL や業務状態永続化は禁止します。

#### 課題10: Read / Write 分離と Dry Run

**背景**

更新系ツールを参照系と同列に扱うと、不可逆な副作用を安全に制御できません。

**対応方針**

* Read ツールと Write ツールを分離して公開します。
* 更新系ツールには Dry Run を標準装備し、差分と影響範囲を返します。
* 本実行は人間承認と AIガバナンス層の最終ゲートを前提にします。

### 3.4 AIガバナンス層の課題

#### 課題11: 入出力 Guardrails とモデル利用統制

**背景**

自然言語の脅威やモデル利用の逸脱は、決定論的な入口防御だけでは扱えません。

**対応方針**

* 入出力 Guardrails を企業共通機構として持ちます。
* モデル利用経路、Tool 利用上限、予算を一元統制します。
* Fast Track と Slow Track で統制強度を切り替えます。

#### 課題12: グラウンディング、サイテーション、共通評価

**背景**

説明責任を満たすには、AI に引用を出させるだけでは足りず、根拠付けと検証をシステムに組み込む必要があります。

**対応方針**

* 根拠データ取得と出典付与を生成プロセスに組み込みます。
* 同期検証で根拠不足のまま次ステップへ進ませないようにします。
* 事後評価で意味妥当性やポリシー適合性を継続的に点検します。

#### 課題13: Risk-Adaptive 監督と Kill Switch

**背景**

自律型や長時間処理を人間が常時監視する前提はスケールしません。

**対応方針**

* 共通評価基盤や LLM-as-a-Judge を活用し、低スコア時のみ HITL へ接続します。
* 危険兆候時には Kill Switch や予算遮断で停止できるようにします。
* Judge は前段機構であり、最終責任は人間または企業ポリシー側に残します。

### 3.5 周辺実行基盤の課題

#### 課題15: Event Bus、状態管理DB、Checkpointer、Observability の分離

**背景**

AI システムでは、状態、イベント、監査、再開が混ざりやすく、ひとつのストアへ押し込むと責務が崩れます。

**対応方針**

* Event Bus は状態変化の事実通知を担います。
* 状態管理DB は UI が参照する現在状態を担います。
* Checkpointer は Application層内部の再開用状態を担います。
* Observability 基盤は `trace_id` を軸に Trajectory を再構成します。

---

## 4. 更新時の判断ルール

本書を今後更新する際は、次のルールに従います。

1. 一次情報は `docs/01_アーキテクチャ検討` 配下を基準にする。
2. 課題は PoC の進捗ではなく、アーキテクチャ論点として定義する。
3. 課題には必ずカテゴリを付け、どの層または全体課題かを明示する。
4. 周辺実行基盤を 3 層へ無理に押し込まない。
5. マルチエージェント時の East-West 統制を独立した論点として扱う。

---

## 5. 関連文書

* 全体配置: [./01_生成AI基盤のコンポーネント配置と実装・運用原則.md](./01_生成AI基盤のコンポーネント配置と実装・運用原則.md)
* Application層の実現方式: [./02_生成AIアプリケーション層の実現方式.md](./02_生成AIアプリケーション層の実現方式.md)
* Tool層の実現方式: [./03_生成AIツール層の実現方式.md](./03_生成AIツール層の実現方式.md)
* AIガバナンス層の実現方式: [./04_生成AIガバナンス層の実現方式.md](./04_生成AIガバナンス層の実現方式.md)
* 一次文書: [../01_アーキテクチャ検討/01_AIエージェントの業務適用を見据えた生成AIアーキテクチャ検討.md](../01_アーキテクチャ検討/01_AIエージェントの業務適用を見据えた生成AIアーキテクチャ検討.md)

---
### 課題12：Write系ツールのDry Run

LLMのハルシネーションや破壊的実行を防ぐための規約。

* **Dry Runの実装回避**：開発者にシミュレーション実装を強制しない。ツール呼び出しのペイロード（AIの意図）をインターセプトして人間に承認させるか、Terraformのように「Plan」と「Apply」のツールを物理的に分離する設計を推奨する。

---
### 課題13：LangGraph Stateのスキーマ進化

コード更新による、フリーズ中のState復元エラーを防ぐ。

* **Version Tagging**：Stateに `_schema_version` を持たせ、構造変更時にインクリメントする。
* **State Migration**：旧バージョンを最新スキーマへ変換するアダプター関数を実装するか、メジャーアップデート時は旧ワーカーを並行稼働（Blue/Green）させて古いスレッドを消化する。

---

## 4. 開発・運用を担保する「ゲート機構」（CI/CD & Runtime）

規約を個人の努力に依存せず、システム的に強制・誘導する仕組み。

1. **ゴールデンパス（自動的に正しい実装へ誘導）**：
* `langfuse.get_prompt()`、`traceparent` 伝播、SSE接続をデフォルトで内包した標準SDK/テンプレート（Scaffold）を提供する。


2. **CI/CDゲート（未対応はデプロイ拒否）**：
* **静的解析**：禁止APIの直叩きや、プロンプトのハードコードを検知してFailさせる。
* **構造検査**：Pydanticの `description` 必須化、StateのJSONシリアライズ可能性、`_schema_version` の存在をテストで検査。


3. **実行時ゲート（最後の砦）**：
* `traceparent` が無いリクエストの拒否/強制採番、およびLiteLLM Proxyを経由しない不正なLLM通信のネットワーク遮断（Egress制御）。



---

## 5. 実装・設定 TODOリスト

上記アーキテクチャを実現するための、担当領域別のアクションアイテムです。

### 5.1 プラットフォーム・インフラ構築（システム側）

* [x] **DockerによるZitadel(IdP)サーバーの構築**:
* [x] **DockerによるLangfuseサーバーの構築**:
* [x] **DockerによるLiteLLM Proxyサーバーの構築**:
* [x] **DockerによるDifyサーバーの構築**:

* [ ] **LiteLLM設定（`config.yaml`）**:
* [ ] `mcp_server` オプションにて `auth_type: oauth2` （MCP OAuth）を設定。
* [ ] `litellm_settings.callbacks` に `["langfuse"]` を指定し統合監視を有効化。
* [x] `general_settings.custom_callbacks` に自作Pythonフック（Guardrail）を登録。


* [ ] **AIガバナンス層 フックの実装（Python）**:
* [x] `pre_api_call_hook`: 入力検査・機密検知・違反時例外スロー（Null考慮済）。
* [ ] `post_api_call_hook`: DLPチェック・フォーマット検証・応答書き換え。
* [ ] `success_callbacks`: Ragas 評価ワーカーへのキュー送信。


* [ ] **サンドボックス制御基盤**: Ephemeralコンテナの起動/Shreddingロジック、およびウォームプールの管理ロジック構築。

### 5.2 エージェント・ツール開発（実装側）

* [x] **非同期バックエンドAPIの実装**: FastAPI + LangGraph を用い、即時応答、ステート永続化、Webhook通知を備えたAPIを構築する。
* [ ] **Difyワークフローの構築**: 上記APIを利用し、DifyのUI上で「HTTPリクエスト ➔ 承認分岐 ➔ 再開」を行うフロントエンドフローを構築する。
* [ ] **プロンプトの外部化**: `langfuse.get_prompt()` 等を利用し、ソースコード内に指示を直接記述しない。
* [ ] **W3C `traceparent` の伝搬**: Difyから受け取ったトレースIDを全てのログ出力・後続API呼び出しに付与する。
* [ ] **Authentication `Bearerトークン` の伝搬**: Difyログイン時にIdPからBearerトークンを生成、Tool層のMCPツールへの伝搬を行う。
* [ ] **メタデータ付きスキーマ定義**: ツールの引数/戻り値の `description` に、データの単位や業務定義を極大化して記述する。

### 5.3 ガバナンス・運用（Governance & Ops）

* [ ] **Langfuse運用フロー**: `production` / `staging` タグを用いたプロンプトのリリース管理手順の策定。
* [ ] **継続的評価サイクル**: Ragas の自動評価スコアに基づく、定期的なプロンプト改善レビュー会議の設置。
* [ ] **Blue/Greenデプロイ手順**: Stateのメジャーアップデート時に、旧ワーカーを並行稼働させてスレッドを消化する運用フローの確立。

---

## 6. ロードマップ：PoCに向けた第一歩

1. **Phase 1 (Infrastructure)**：LiteLLM ProxyとLangfuseのセットアップ、およびHTTPベースのMCP通信路（OAuth統合）の確立。
2. **Phase 2 (Logic)**：Difyの「3分割ワークフロー」を用いた、読み取り専用ツールの非同期RAG実装。
3. **Phase 3 (Write/Auth)**：非同期HITLとトークン再注入を用いた、安全な書き込み系ツールの実装。
4. **Phase 4 (Autonomy)**：ラッパーAPIによるサンドボックス制御と、Codex/Claude Codeの統合（コールドスタート対策含む）。
