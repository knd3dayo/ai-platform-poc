# 生成AI基盤インフラ構築手順

* Docker環境のセットアップ: 後述のソフトウェア、アプリケーションの実行環境の構築

* PostgreSQLのセットアップ: Langfuse、Dify、litellm、langgraph、状態管理DBなどで使用するDBとして用意。
* Redisのプロビジョニング: Event Bus、Langfuse、Difyなどで使用するEvent Busとして用意。

* Langfuseのセットアップ: 全ての実験ログがここに入るようにする。
* Difyのセットアップ: 「WF型」のアプリケーション実行環境。
* LiteLLM Proxyのデプロイ: 外部LLM（Azure OpenAI等）への接続を集約し、APIキーを隠蔽。

* サンドボックス環境のセットアップ: 「自律型」のアプリケーション実行環境。
* (PoCのMCPバックエンド用): Denodo環境構築
* (PoC前のテスト用): IdP(Zitadel)環境構築

## 設定一覧

### ネットワーク設定
* docker network(docker)  
ai_platform_net
* postgresql(docker)  
postgres 5432
* redis(docker)    
redis 6379, 8001(redis insight)
* langfuse(docker)    
langfuse-web 3000
minio 9000
* Dify(docker)    
sandbox 8194 (コンテナ内部)
plugin_daemon 5002, 5003 (コンテナ内部)
nginx 8081 (ホスト公開)
* litellm(docker)    
litellm 4000

* Denodo(host)  
VDP 9999
Webツール 9090, 9099, 9098, 9097

* Denodo AI SDK(host)   
api 8008
chatbot 9992

* 自律エージェント用APIサーバー(host) ※ Dockerを使用するためhostで実行
Cline用api 7101  
* SV型エージェント用APIサーバー(host) 　
api 7201

* BFF
BFF用APIサーバー 7301


## 事前準備

以下のツールがインストールされていることを確認してください。

* **Docker / Docker Compose**
* **Git**
* **`uv`** (Pythonパッケージマネージャー)

```bash
cd ai-platform-poc

# 手順書内で使う「リポジトリルート」
export AI_PLATFORM_POC_ROOT="$PWD"

# 後続手順で参照する環境変数（ai-platform-samplelib の絶対パス）
export AI_PLATFORM_LIB="$AI_PLATFORM_POC_ROOT/ai-platform-samplelib"

```
## 共通ネットワーク構築
```bash
cd "$AI_PLATFORM_POC_ROOT/00-network"
docker compose up -d
```

## 共通PostgreSQLの構築
```bash
cd "$AI_PLATFORM_POC_ROOT/01-postgresql"
docker compose up -d 
```

## 共通Redisの構築
```bash
cd "$AI_PLATFORM_POC_ROOT/02-redis"
docker compose up -d 
```

## Langfuseの構築
```bash
cd "$AI_PLATFORM_POC_ROOT/03-langfuse"
# ClickHouse用：所有者をコンテナ内の実行ユーザー（101）に変更
mkdir -p ../data/clickhouse/data ../data/clickhouse/logs
sudo chown -R 101:101 ../data/clickhouse

# Minio用：全てのユーザーに書き込み権限を付与（最も確実な方法）
mkdir -p ../data/minio
sudo chmod -R 777 ../data/minio
```

```bash
docker compose up -d 
```

## (EntraIDがない場合) Zitadel導入とBFF～MCPへの一連の流れでのBearerトークンの伝搬

### Zitadelの構築 (Docker)

このリポジトリでは [91-zitadel/docker-compose.yml](91-zitadel/docker-compose.yml) を同梱済みです（`ai_platform_net` への接続設定も含みます）。

```bash
cd "$AI_PLATFORM_POC_ROOT/91-zitadel"
```

```bash
docker compose up -d
docker compose ps
```

ブラウザで `http://localhost:8080/ui/console` にアクセスしてログインしてください。

NOTE: 初期ログイン名/パスワードは [91-zitadel/docker-compose.yml](91-zitadel/docker-compose.yml) の `ZITADEL_FIRSTINSTANCE_*` 設定に依存します。ログインできない場合は、compose内コメントにある「利用可能なログイン名をDBから確認する」手順を参照してください。

---

### 初期設定（コンソールへのログイン）

1. ブラウザで `http://localhost:8080/ui/console` を開きます。
2. 初期ユーザー/パスワードは [91-zitadel/docker-compose.yml](91-zitadel/docker-compose.yml) の設定に従ってログインします。
---

### ZitadelでのPythonテストクライアント用のキー作成手順

Zitadelのコンソールで、必要なら **Projects** を作成し、テストクライアント用の Application を追加します。

#### 設定1：Pythonスクリプト用（Machine-to-Machine）

ブラウザを介さず、スクリプトが直接認証するための設定です。

1. **Applications** ➔ **Create** をクリック。
2. **Name**: `mcp-test-client`
3. **Type**: **`API`** を選択。
4. **Auth Method**: **`Basic`** を選択。
5. **情報の保存**:
* 表示される **`Client ID`** と **`Client Secret`** をメモし、Pythonコードの変数にセットします。


---

### ステップ4：DifyとLiteLLM Proxyの接続

1. `http://localhost:8080` (Dify) にアクセス。
2. **Settings > Model Providers > OpenAI-API-compatible** を追加。
* **Endpoint URL**: `http://litellm:4000`
* **API Key**: `sk-poc-master-key-12345`

---

## Difyの構築
```bash
# 共通Postgresに対してDB作成コマンドを実行
docker exec -it postgres psql -U postgres -c "CREATE DATABASE dify;"
# 共通Postgres内に dify_plugin というDBを作る
docker exec -it postgres psql -U postgres -c "CREATE DATABASE dify_plugin;"

cd "$AI_PLATFORM_POC_ROOT/04-dify/docker"
docker compose up -d 
```

---
---

## LiteLLM Proxyの構築

```bash
cd "$AI_PLATFORM_POC_ROOT/05-litellm"

# （任意）ローカルでPython側の動作確認をする場合
# uv sync

```

### 設定ファイルの作成

**`.env`**

```env
LITELLM_MASTER_KEY=sk-poc-master-key-12345
OPENAI_API_KEY=sk-your-openai-api-key
LANGFUSE_PUBLIC_KEY=pk-lf-... # Langfuseのキー
LANGFUSE_SECRET_KEY=sk-lf-... # Langfuseのキー

```

**`docker-compose.yml`**
このリポジトリの実体は [05-litellm/docker-compose.yml](05-litellm/docker-compose.yml) です。

- `LANGFUSE_HOST` は compose 側で `http://langfuse-web:3000` に固定しています（`.env` に定義は不要）

```yaml
services:
  litellm:
    restart: always
    image: ghcr.io/berriai/litellm:main-latest
    environment:
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LANGFUSE_PUBLIC_KEY=${LANGFUSE_PUBLIC_KEY}
      - LANGFUSE_SECRET_KEY=${LANGFUSE_SECRET_KEY}
      - LANGFUSE_HOST=http://langfuse-web:3000
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./src/ai_platform_litellm:/app/ai_platform_litellm
    ports:
      - "4000:4000"
    command: [ "--config", "/app/config.yaml", "--detailed_debug" ]
    extra_hosts:
      - "localhost:host-gateway"

networks:
  default:
    name: ai_platform_net
    external: true

```
### 起動
```bash
docker compose up -d
```

### テスト
```
curl -X POST 'http://localhost:4000/v1/chat/completions' \
-H 'Authorization: Bearer sk-poc-master-key-12345' \
-H 'Content-Type: application/json' \
-d '{
    "model": "gpt-4o",
    "messages": [
        {"role": "user", "content": "LiteLLMの疎通確認テストです。短い挨拶を返してください。"}
    ]
}'
```

```bash
uv --directory ${AI_PLATFORM_LIB} run -m ai_platform_samplelib.tool.test_client.client

```

---
## Cline用Sandboxの構築

このリポジトリでは、`docker-compose.yml` 側で `build:` を行わず、各イメージディレクトリの `build.sh` で事前に `docker build` します。

- 目的: `ai-platform-samplelib` をイメージに含めるため（ビルドコンテキスト外は `COPY` できないため、`build.sh` が一時的に build context 配下へコピーします）
- 前提: `images/*/.env` の `AI_PLATFORM_LIB` が正しいパスを指していること（デフォルトは `../../../../ai-platform-samplelib`）

```bash
cd "$AI_PLATFORM_POC_ROOT/12-application/autonomous-agent-executor/images/cline-image"

# イメージを事前ビルド（docker compose build は不要）
./build.sh

```
### テスト
```bash
docker compose run --rm cline  cline -y "このプロジェクトを日本語で説明して"
```

---
## Claude Code用Sandboxの構築

```bash
cd "$AI_PLATFORM_POC_ROOT/12-application/autonomous-agent-executor/images/claude-code-image"

./build.sh

# 例: CLI起動（詳細は claude 側のヘルプに従ってください）
docker compose run --rm claude-code-executor claude
```

---
## OpenCode用Sandboxの構築

```bash
cd "$AI_PLATFORM_POC_ROOT/12-application/autonomous-agent-executor/images/open-code-image"

./build.sh

# コンテナ内で CLI を実行（まずはシェル起動が確実）
docker compose run --rm open-code-executor /bin/bash
```

### APIサーバー&クライアントのテスト
APIサーバーを起動
```bash
cd "$AI_PLATFORM_POC_ROOT/12-application/autonomous-agent-executor/cli"
. ${AI_PLATFORM_LIB}/.venv/bin/activate
# cline/claude のどちらで動かすかに応じて env-file を切り替える
python -m ai_platform_samplelib.application.autonomous.api.main -e ./.env_cline
```

Pythonコードと修正の指示をAPIサーバーに送り、Cline CLI用コンテナで処理するテストクライアント
```bash
. ${AI_PLATFORM_LIB}/.venv/bin/activate
python -m ai_platform_samplelib.application.autonomous.test_client.client
```

ソースコード一式をZIPファイルに固めて、Cline CLI用コンテナで処理するテストクライアント
```bash
. ${AI_PLATFORM_LIB}/.venv/bin/activate
python -m ai_platform_samplelib.application.autonomous.test_client.client2
```

APIサーバーではなくCLIでCline CLI用コンテナで処理するテストクライアント
12-application/autonomous-agent-executor/cli
```bash
./test_client.sh [cline|claude]

```

## Codex用Sandboxの構築
(作成中)

### LangGraphによるSV型エージェントの構築
サーバ起動（Docker）
```bash
cd "$AI_PLATFORM_POC_ROOT/12-application/sv-agent-executor"
docker compose up -d
```

（代替）サーバ起動（ホストで直接起動する場合）
※ 上の `docker compose up -d` と同時に実行するとポート競合するので、どちらか一方にしてください。
```bash
. ${AI_PLATFORM_LIB}/.venv/bin/activate
python -m ai_platform_samplelib.application.super_visor.api.api_server
```

```bash
. ${AI_PLATFORM_LIB}/.venv/bin/activate
 python -m ai_platform_samplelib.application.super_visor.test_client.client --api-url http://localhost:7201 --message 'hello from client' --max-wait-sec 30 2>&1
```

CLI版
```bash
. ${AI_PLATFORM_LIB}/.venv/bin/activate
 python -m ai_platform_samplelib.application.super_visor.cli.main
```

## Denodo Expressの構築
```bash
docker pull harbor.open.denodo.com/denodo-express/denodo-platform@sha256:7c55969a884def1bfbc71ed9cd8bfb1c618a18619dd526bb1d42eec90031ec3b
```

## Denodo AI SDKの構築
```bash
cd "$AI_PLATFORM_POC_ROOT/06-denodo/denodo-ai-sdk/denodo-ai-sdk"

# 例: 依存関係の導入
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt

# 例: API + サンプルチャットボット起動
python run.py both
```

