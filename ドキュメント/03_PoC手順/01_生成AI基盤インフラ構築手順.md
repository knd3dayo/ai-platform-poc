# 生成AI基盤インフラ構築手順

* Docker環境のセットアップ: 後述のソフトウェア、アプリケーションの実行環境の構築
* Langfuseのセットアップ: 全ての実験ログがここに入るようにする。
* Difyのセットアップ: 「WF型」のアプリケーション実行環境。
* サンドボックス環境のセットアップ: 「自律型」のアプリケーション実行環境。
* LiteLLM Proxyのデプロイ: 外部LLM（Azure OpenAI等）への接続を集約し、APIキーを隠蔽。
* Redisのプロビジョニング: Event Busと状態管理DBとしての箱を用意。
* (PoCのMCPバックエンド用): Denodo環境構築
* (PoC前のテスト用): IdP(Zitadel)環境構築

## 事前準備

以下のツールがインストールされていることを確認してください。

* **Docker / Docker Compose**
* **Git**
* **`uv`** (Pythonパッケージマネージャー)

```bash
mkdir ai-platform-poc
cd ai-platform-poc

```
# 共通PostgreSQLの構築

mkdir 01-postgesql
cd 01-postgresql


---
# Zitadel導入とBFF～MCPへの一連の流れでのBearerトークンの伝搬

## ステップ0：Zitadelの構築 (Docker)

既存の `ai-platform-poc` ディレクトリ内に作成します。

```bash
mkdir 0_zitadel
cd 0_zitadel

```

Zitadelリポジトリからファイルをダウンロードしてください:docker-compose.yaml
```bash
curl -L https://raw.githubusercontent.com/zitadel/zitadel/main/apps/docs/content/self-hosting/deploy/docker-compose.yaml -o docker-compose.yml
```

**`0_zitadel/docker-compose.yml`**

```yaml
services:
  zitadel:
    # ... 既存の設定 ...
    networks:
      - default
      - ai_platform_net  # 追加

  db:
    # ... 既存の設定 ...
    networks:
      - default
      - ai_platform_net  # 追加

networks:
  ai_platform_net:
    external: true

```

```bash
docker compose up -d
docker compose ps
```

http://localhost:8080/ui/console?login_hint=zitadel-admin@zitadel.localhost にアクセスしてログインしてください。
パスワード：Password1!

---

## 4. 初期設定（コンソールへのログイン）

1. ブラウザで `http://localhost:8080/ui/console` を開きます。
2. 以下の認証情報でログインします。
* **Username**: `zitadel-admin@zitadel.localhost`
*(※ドメイン部分は `ZITADEL_EXTHOST` の設定により変わります)*
* **Password**: `Password1!`
---

# ZitadelでのPythonテストクライアント用のキー作成手順

まずは、Zitadelのコンソールで **Projects** ➔ **`AI-Platform`** （作成済みのプロジェクト）を開いてください。

### 設定1：Pythonスクリプト用（Machine-to-Machine）

ブラウザを介さず、スクリプトが直接認証するための設定です。

1. **Applications** ➔ **Create** をクリック。
2. **Name**: `mcp-test-client`
3. **Type**: **`API`** を選択。
4. **Auth Method**: **`Basic`** を選択。
5. **情報の保存**:
* 表示される **`Client ID`** と **`Client Secret`** をメモし、Pythonコードの変数にセットします。

---

## ステップ1：統合監視・プロンプトCMS（Langfuse）の構築

公式リポジトリをクローンして構築します。これがネットワークの起点となります。

### 1-1. リポジトリのクローンと環境設定

```bash
# 公式リポジトリを 1_langfuse としてクローン
git clone https://github.com/langfuse/langfuse.git 1_langfuse
cd 1_langfuse
```

### 1-2. `docker-compose.yml` の編集（ネットワーク設定）

Langfuseの `docker-compose.yml` をエディタで開き、末尾にネットワーク定義を追加（または修正）して、**全サービスが共通ネットワークに参加**するようにします。

```yaml
# 1_langfuse/docker-compose.yml の末尾に追加

networks:
  default:
    name: ai_platform_net
    driver: bridge

```

> [!NOTE]
> これにより、Langfuse内の `langfuse-web` や `postgres` が自動的に `ai_platform_net` に所属し、後続のLiteLLMから名前解決が可能になります。

### 1-3. 起動とAPIキーの取得

1. `docker compose up -d` を実行します。
2. `http://localhost:3000` にアクセスし、新規アカウント作成（初回ユーザーが管理者になります）。
3. プロジェクト設定から **Public Key** と **Secret Key** を生成し、メモします。

---

## ステップ2：AI Assurance層（LiteLLM Proxy）の構築

### 2-1. プロジェクト初期化

```bash
cd ..
mkdir 2_litellm
cd 2_litellm
uv sync

```

### 2-2. 設定ファイルの作成

**`2_litellm/.env`**

```env
LITELLM_MASTER_KEY=sk-poc-master-key-12345
OPENAI_API_KEY=sk-your-openai-api-key
LANGFUSE_PUBLIC_KEY=pk-lf-... # メモしたキー
LANGFUSE_SECRET_KEY=sk-lf-... # メモしたキー

```

**`2_litellm/docker-compose.yml`**
公式Langfuse構成に合わせ、`LANGFUSE_HOST` はサービス名である **`langfuse-web`** を指定します。

```yaml
services:
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    environment:
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LANGFUSE_PUBLIC_KEY=${LANGFUSE_PUBLIC_KEY}
      - LANGFUSE_SECRET_KEY=${LANGFUSE_SECRET_KEY}
      - LANGFUSE_HOST=http://langfuse-web:3000 
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./src/ai_platform_litellm:/app/ai_platform_litellm
    ports:
      - "4000:4000"
    command: [ "--config", "/app/config.yaml" ]
    networks:
      - ai_platform_net

networks:
  ai_platform_net:
    external: true

```
### 起動
```bash
docker compose up -d
```

### 2-3. テスト
```
curl -X POST 'http://localhost:4000/v1/chat/completions' \
-H 'Authorization: Bearer sk-poc-master-key-12345' \
-H 'Content-Type: application/json' \
-d '{
    "model": "gpt-4o",
    "messages": [
        {"role": "user", "content": "LiteLLMの疎通確認テストです。短い挨拶を返してください。"}
    ]
}'
```
---

## ステップ3：アプリケーション層（Dify）の構築

### 3-1. Difyのクローンと初期設定

```bash
cd ..
git clone https://github.com/langgenius/dify.git 3_dify
cd 3_dify/docker
cp .env.example .env

```

### 3-2. `.env` と `docker-compose.yaml` の修正

1. **`.env`**: `EXPOSE_NGINX_PORT=8080` に変更。
2. **`docker-compose.yaml`**: 末尾のネットワーク定義を修正。

```yaml
# 3_dify/docker/docker-compose.yaml 末尾
networks:
  default:
    name: ai_platform_net
    external: true

```

### 3-3. 起動

```bash
docker compose up -d

```

---

## ステップ4：DifyとLiteLLM Proxyの接続

1. `http://localhost:8080` (Dify) にアクセス。
2. **Settings > Model Providers > OpenAI-API-compatible** を追加。
* **Endpoint URL**: `http://litellm:4000/v1`
* **API Key**: `sk-poc-master-key-12345`

Zitadelを選択いただきありがとうございます。ZitadelはGo製でリソース消費が少なく、かつ設定画面も直感的なため、今回のPoC環境には最適です。

Zitadelを認証基盤（IdP）として、LiteLLMをMCP Gateway（リソースサーバー）として構築し、PythonクライアントからOIDC（OAuth 2.1）で認証・アクセスを行う一連の手順をまとめました。

---

